{
  "schema_version": 1,
  "rules": {
    "wip_limit": 1,
    "allowed_states": [
      "active",
      "blocked",
      "not_started",
      "passing"
    ],
    "passing_requires": "The verification command succeeds and evidence is recorded."
  },
  "bootstrap_evidence": {
    "date": "2026-05-27",
    "command": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
    "result": "passed: ruff check, pytest smoke tests (2 passed), and harness validation"
  },
  "features": [
    {
      "id": "F01",
      "behavior": "Capture an Agent run timeline with reasoning steps, tool calls, token usage, latency, and errors.",
      "verification": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
      "state": "passing",
      "evidence": "2026-05-27: Implemented AgentRun and RunEvent schemas, SQLite append-only event storage, POST/GET /v1/runs, POST/GET /v1/runs/{run_id}/events. Validation passed with ruff, 10 pytest tests, and harness validation via scripts/check.ps1."
    },
    {
      "id": "F02",
      "behavior": "Record RAG retrieval evidence with query, chunks, source metadata, hit or miss signal, and citation coverage.",
      "verification": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
      "state": "passing",
      "evidence": "2026-05-27: Implemented RagEvidence, RetrievedChunk, Citation, and RagHitStatus schemas with validation for citation references and hit/miss consistency. Added POST /v1/runs/{run_id}/rag/evidence, which persists evidence as a rag_retrieval timeline event. Validation passed with ruff, 20 pytest tests, and harness validation via scripts/check.ps1."
    },
    {
      "id": "F03",
      "behavior": "Run automated answer quality checks for hallucination risk, groundedness, and trustworthiness.",
      "verification": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
      "state": "passing",
      "evidence": "2026-05-28: Implemented EvaluationResult, EvaluationMetric, metric names, metric directions, and pass/warn/fail verdict computation for groundedness, citation accuracy, hallucination risk, and trustworthiness. Added POST /v1/runs/{run_id}/evaluations, which persists evaluation results as evaluation timeline events. Validation passed with ruff, 30 pytest tests, and harness validation via scripts/check.ps1."
    },
    {
      "id": "F04",
      "behavior": "Compare evaluation results across prompt or code changes to detect regressions.",
      "verification": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
      "state": "passing",
      "evidence": "2026-05-28: Implemented regression comparison schemas and POST /v1/regressions/compare. Baseline and candidate evaluation results are compared with prompt/model/version metadata, metric-level quality deltas, tolerance-based regression detection, and improved/unchanged/regressed status. Validation passed with pytest schema/API coverage before documentation update."
    },
    {
      "id": "F05",
      "behavior": "Define a developer-facing run detail contract showing what the Agent thought, called, spent, and where it failed.",
      "verification": "powershell -ExecutionPolicy Bypass -File scripts/check.ps1",
      "state": "not_started",
      "evidence": "Planned product capability seeded during framework bootstrap; implementation not started."
    }
  ]
}
